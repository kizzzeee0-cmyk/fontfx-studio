# Changelog

## v1.3.0

### Added
- 글자 뒤에 칠한 **붙는 드로잉**도 그룹 외곽선 계산에 포함되도록 개선했습니다. 이제 글자/그룹을 선택하고 뒤에 칠한 뒤 그룹화하여 획을 추가하면, **그린 영역 바깥까지 함께 획이 생성**됩니다.

### Improved
- 그룹 효과용 마스크 계산에 **아래쪽(behind) 드로잉**을 함께 합성하도록 수정했습니다.
- 그룹에 붙어 있는 드로잉의 아래/위 렌더 순서를 정리해, `선은 글자 아래에 표시` 상태가 더 일관되게 보이도록 개선했습니다.

## v1.2.9

### Added
- 선택한 **글자/그룹에 붙는 드로잉** 기능 추가: 글자나 같은 그룹을 선택한 뒤 그리기 모드로 선을 그리면 해당 대상에 선이 고정되어 함께 이동합니다.
- **브러시 종류** 추가: 펜 / 형광펜 / 에어브러시
- **선 보정 모드** 추가: 자유선 / 직선 보정 / 곡선 보정 / 원형 보정

### Improved
- 선택 글자 위에 그린 선이 해당 글자와 시각적으로 더 자연스럽게 합쳐져 보이도록 렌더 순서를 조정했습니다.
- 글자별 PNG 내보내기에서도 해당 글자에 붙어 있는 드로잉이 함께 저장되도록 개선했습니다.
- 정지 보정 및 드로잉 종료 보정이 직선/곡선/원형 보정 모드와도 함께 작동하도록 개선했습니다.

## v1.2.8

### Added
- 그리기 모드에서 선을 그린 뒤 **마우스를 누른 채 2초간 머무르면**, 현재 선을 한 번 더 부드럽게 보정하는 **정지 보정 기능**이 추가되었습니다.

### Improved
- 자유 드로잉 선 정리 로직을 보완해 삐뚤빼뚤한 선이 더 자연스럽게 다듬어지도록 개선했습니다.
- 드로잉 종료 시에도 약한 후처리 보정을 적용해 선이 조금 더 매끈하게 마무리되도록 했습니다.

## v1.2.7

### Added — 편집 미리보기 확대/축소
- 실제 PNG 출력 크기와 분리된 **미리보기 줌 5%~400%** 기능을 추가했습니다.
- `− / ＋` 버튼, 줌 슬라이더, 현재 배율 표시 버튼을 추가했습니다.
- 배율 표시(`100%`)를 누르면 즉시 100%로 돌아옵니다.
- `화면 맞춤`은 캔버스를 작업 영역에 자동으로 맞춥니다.
- `Ctrl + 마우스 휠`로 커서 위치를 기준으로 확대/축소할 수 있습니다.
- 키보드 `+ / - / 0`으로 확대 / 축소 / 100% 복귀가 가능합니다.

### Improved
- 큰 배율에서도 편집 캔버스의 내부 픽셀 버퍼가 과도하게 커지지 않도록 미리보기 렌더 버퍼를 자동 제한했습니다.
- 따라서 300~400% 확대 상태에서도 브라우저 메모리 사용량이 불필요하게 폭증하는 것을 방지합니다.
- 확대 상태에서도 그리기 좌표는 원본 캔버스 좌표로 정확히 환산됩니다. 최종 PNG 크기에는 줌 배율이 영향을 주지 않습니다.

## v1.2.6

### Added — 가져온 폰트 자동 저장 / 자동 복원
- 웹 CSS 폰트, 직접 URL 폰트, 로컬 TTF/OTF/WOFF/WOFF2 폰트를 브라우저에 자동 저장합니다.
- 같은 사이트 주소로 다시 접속하면 저장된 폰트를 자동 복원합니다.
- 로컬 폰트 파일은 IndexedDB에 파일 데이터 자체를 저장하여 재업로드 없이 다시 사용할 수 있습니다.
- 폰트 관리 목록에 `자동 저장` 상태를 표시합니다.
- 가져온 폰트를 삭제하면 브라우저에 저장된 사본도 함께 삭제합니다.

### Notes
- 자동 저장은 **같은 브라우저 + 같은 사이트 주소(Origin)** 기준입니다.
- 브라우저 사이트 데이터를 삭제하거나 Cloudflare/GitHub Pages 주소를 바꾸면 저장소가 달라질 수 있습니다.
- 브라우저 저장공간 제한을 초과하면 일부 대용량 로컬 폰트는 저장되지 않을 수 있습니다.

## v1.2.5

### Added
- **그리기 툴** 추가: 브러시 색상 선택, 브러시 굵기 슬라이더, 손떨림 방지 강한 자유 드로잉 지원
- 드로잉 선을 **글자 위 / 글자 아래**로 배치 전환 가능
- `마지막 선 삭제`, `전체 선 삭제` 버튼 추가
- **가로 중앙 / 세로 중앙 / 정중앙 정렬 버튼** 추가

### Improved
- 다중 획 / 그룹 합체 획 편집 시 렌더링 품질과 속도의 균형을 다시 조정해 체감 렉을 완화했습니다.
- 그룹 효과 미리보기의 샘플링 해상도와 외곽선 확장 계산량을 줄여 더 빠르게 반응하도록 최적화했습니다.
- 효과 편집 중 불필요하게 레이어 목록을 자주 다시 그리던 동작을 줄였습니다.
- 정렬 기준을 단순 중심점 평균이 아니라 **선택된 글자 전체 경계 박스 기준 중앙 정렬**로 개선했습니다.

## v1.2.4

### Added — 획 / 내부 그림자 프리셋 라이브러리
- 현재 설정한 **획 전체 스택**을 이름을 붙여 저장할 수 있습니다.
- 여러 개의 획을 저장하면 개수, 순서, 굵기, 위치, 혼합모드, 불투명도, 색상이 모두 보존됩니다.
- 현재 설정한 **내부 그림자 전체 스택**도 별도 프리셋으로 저장할 수 있습니다.
- 내부 그림자의 혼합모드, 색상, 불투명도, 각도, 거리, 경계감소, 크기/블러, 윤곽선, 품질이 모두 보존됩니다.
- 저장한 프리셋은 다른 글자나 합체 그룹에 한 번에 적용할 수 있습니다.
- 같은 이름으로 다시 저장하면 기존 프리셋을 갱신합니다.
- 저장 프리셋을 개별 삭제할 수 있습니다.
- 프리셋은 브라우저 `localStorage`에 저장되어 같은 사이트 주소에서 재접속해도 유지됩니다.
- 프로젝트 `.fontfx.json`에도 프리셋 라이브러리가 포함되어 다른 환경에서 프로젝트를 열 때 병합됩니다.

### Kept
- v1.2.3의 그룹 합체 획/내부 그림자 성능 최적화를 그대로 유지합니다.

## v1.2.3

### Improved
- 선택 글자 그룹화 후 그룹 획/내부 그림자 편집 시 성능을 최적화했습니다.
- 그룹 효과 렌더링을 전체 캔버스 기준이 아니라 **그룹의 실제 경계 박스 기준**으로 계산하도록 변경했습니다.
- 그룹 효과용 마스크/레이어를 캐시하여 같은 모양의 반복 렌더링 비용을 크게 줄였습니다.
- 그룹 전체를 함께 이동할 때도 상대 배치 기준으로 캐시를 재사용하도록 최적화했습니다.

### Fixed
- 그룹 획 편집 시 로딩이 길어지고 렉이 걸리던 문제를 완화했습니다.
- 웹 배포용 HTML의 버전 쿼리를 `1.2.3`으로 갱신했습니다.

## v1.2.2

### Webfont loader overhaul
- Google Fonts의 전체 `<link>` 복사 코드를 붙여넣어도 실제 stylesheet 링크를 자동 선택합니다.
- `@import url(...)`, CSS URL, `@font-face { ... }` 원문 CSS를 모두 지원합니다.
- Google Fonts URL의 `family=` 값에서 font-family 이름 자동 감지를 시도합니다.
- CORS 허용 CSS는 `fetch`로 직접 읽고, CSS 내부 상대 `url(...)` 경로를 절대 경로로 보정한 뒤 `<style>`로 삽입합니다.
- CSS fetch가 CORS로 차단되면 `<link rel="stylesheet">` 방식으로 자동 재시도합니다.
- 폰트 로드 상태를 성공 / 경고 / 오류로 상세 표시합니다.
- stylesheet는 연결됐지만 FontFaceSet 확인이 늦는 경우 더 이상 곧바로 실패 처리하지 않고 목록에 등록해 미리보기로 확인할 수 있게 했습니다.
- 웹 CSS의 family 이름 입력을 선택 사항으로 변경했습니다.
- 프로젝트에 직접 붙여넣은 `@font-face` CSS도 다시 불러올 수 있게 저장/복원 구조를 보강했습니다.
- HTML의 app.js / styles.css 버전 쿼리를 1.2.2로 변경해 Cloudflare/브라우저의 이전 JS 캐시가 남는 문제를 방지했습니다.

## v1.2.1
- Cloudflare build script에서 `.nojekyll` 누락 시 실패하던 문제 수정

## v1.2.0
- 글자 채우기 선형/원형 그라데이션 및 다중 색상 스톱 추가
