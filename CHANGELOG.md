# Changelog

## v1.2.4

### Added — 획 / 내부 그림자 프리셋 라이브러리
- 현재 설정한 **획 전체 스택**을 이름을 붙여 저장할 수 있습니다.
- 여러 개의 획을 저장하면 개수, 순서, 굵기, 위치, 혼합모드, 불투명도, 색상이 모두 보존됩니다.
- 현재 설정한 **내부 그림자 전체 스택**도 별도 프리셋으로 저장할 수 있습니다.
- 내부 그림자의 혼합모드, 색상, 불투명도, 각도, 거리, 경계감소, 크기/블러, 윤곽선, 품질이 모두 보존됩니다.
- 저장한 프리셋은 다른 글자나 합체 그룹에 한 번에 적용할 수 있습니다.
- 같은 이름으로 다시 저장하면 기존 프리셋을 갱신합니다.
- 저장 프리셋을 개별 삭제할 수 있습니다.
- 프리셋은 브라우저 `localStorage`에 저장되어 같은 사이트 주소에서 재접속해도 유지됩니다.
- 프로젝트 `.fontfx.json`에도 프리셋 라이브러리가 포함되어 다른 환경에서 프로젝트를 열 때 병합됩니다.

### Kept
- v1.2.3의 그룹 합체 획/내부 그림자 성능 최적화를 그대로 유지합니다.

## v1.2.3

### Improved
- 선택 글자 그룹화 후 그룹 획/내부 그림자 편집 시 성능을 최적화했습니다.
- 그룹 효과 렌더링을 전체 캔버스 기준이 아니라 **그룹의 실제 경계 박스 기준**으로 계산하도록 변경했습니다.
- 그룹 효과용 마스크/레이어를 캐시하여 같은 모양의 반복 렌더링 비용을 크게 줄였습니다.
- 그룹 전체를 함께 이동할 때도 상대 배치 기준으로 캐시를 재사용하도록 최적화했습니다.

### Fixed
- 그룹 획 편집 시 로딩이 길어지고 렉이 걸리던 문제를 완화했습니다.
- 웹 배포용 HTML의 버전 쿼리를 `1.2.3`으로 갱신했습니다.

## v1.2.2

### Webfont loader overhaul
- Google Fonts의 전체 `<link>` 복사 코드를 붙여넣어도 실제 stylesheet 링크를 자동 선택합니다.
- `@import url(...)`, CSS URL, `@font-face { ... }` 원문 CSS를 모두 지원합니다.
- Google Fonts URL의 `family=` 값에서 font-family 이름 자동 감지를 시도합니다.
- CORS 허용 CSS는 `fetch`로 직접 읽고, CSS 내부 상대 `url(...)` 경로를 절대 경로로 보정한 뒤 `<style>`로 삽입합니다.
- CSS fetch가 CORS로 차단되면 `<link rel="stylesheet">` 방식으로 자동 재시도합니다.
- 폰트 로드 상태를 성공 / 경고 / 오류로 상세 표시합니다.
- stylesheet는 연결됐지만 FontFaceSet 확인이 늦는 경우 더 이상 곧바로 실패 처리하지 않고 목록에 등록해 미리보기로 확인할 수 있게 했습니다.
- 웹 CSS의 family 이름 입력을 선택 사항으로 변경했습니다.
- 프로젝트에 직접 붙여넣은 `@font-face` CSS도 다시 불러올 수 있게 저장/복원 구조를 보강했습니다.
- HTML의 app.js / styles.css 버전 쿼리를 1.2.2로 변경해 Cloudflare/브라우저의 이전 JS 캐시가 남는 문제를 방지했습니다.

## v1.2.1
- Cloudflare build script에서 `.nojekyll` 누락 시 실패하던 문제 수정

## v1.2.0
- 글자 채우기 선형/원형 그라데이션 및 다중 색상 스톱 추가
